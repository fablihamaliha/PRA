═══════════════════════════════════════════════════════════════
  FIX "SITE CAN'T BE REACHED" - CLOUDFLARE TUNNEL SETUP
═══════════════════════════════════════════════════════════════

Problem: https://skincares.work can't be reached
Solution: Set up Cloudflare Tunnel on your Raspberry Pi


🚀 QUICK SETUP (Copy & Paste These Commands)
───────────────────────────────────────────────────────────────

Step 1: SSH to Raspberry Pi
┌─────────────────────────────────────────────────────────────┐
│ ssh pi@192.168.1.68                                         │
└─────────────────────────────────────────────────────────────┘


Step 2: Install cloudflared
┌─────────────────────────────────────────────────────────────┐
│ wget https://github.com/cloudflare/cloudflared/releases/   │
│   latest/download/cloudflared-linux-arm64                   │
│ chmod +x cloudflared-linux-arm64                            │
│ sudo mv cloudflared-linux-arm64 /usr/local/bin/cloudflared │
│ cloudflared --version                                       │
└─────────────────────────────────────────────────────────────┘


Step 3: Login to Cloudflare
┌─────────────────────────────────────────────────────────────┐
│ cloudflared tunnel login                                    │
└─────────────────────────────────────────────────────────────┘

⚠️  This will give you a URL to open in browser
    Login and select domain: skincares.work


Step 4: Create Tunnel
┌─────────────────────────────────────────────────────────────┐
│ cloudflared tunnel create pra-tunnel                        │
└─────────────────────────────────────────────────────────────┘

⚠️  SAVE THE TUNNEL ID that's displayed!
    You'll need it in the next step.


Step 5: Create Config File
┌─────────────────────────────────────────────────────────────┐
│ mkdir -p ~/.cloudflared                                     │
│ nano ~/.cloudflared/config.yml                              │
└─────────────────────────────────────────────────────────────┘

Paste this (replace <TUNNEL_ID> with your actual ID):
┌─────────────────────────────────────────────────────────────┐
│ tunnel: <TUNNEL_ID>                                         │
│ credentials-file: /home/pi/.cloudflared/<TUNNEL_ID>.json   │
│                                                             │
│ ingress:                                                    │
│   - hostname: skincares.work                                │
│     service: http://localhost:5001                          │
│   - hostname: www.skincares.work                            │
│     service: http://localhost:5001                          │
│   - service: http_status:404                                │
└─────────────────────────────────────────────────────────────┘

Save: Ctrl+X, then Y, then Enter


Step 6: Configure DNS (Cloudflare Dashboard)

Open: https://dash.cloudflare.com
Go to: skincares.work > DNS

Add CNAME record #1:
  Type: CNAME
  Name: @
  Target: <TUNNEL_ID>.cfargotunnel.com
  Proxy: ON (orange cloud)

Add CNAME record #2:
  Type: CNAME
  Name: www
  Target: <TUNNEL_ID>.cfargotunnel.com
  Proxy: ON (orange cloud)


Step 7: Test Tunnel
┌─────────────────────────────────────────────────────────────┐
│ cloudflared tunnel run pra-tunnel                           │
└─────────────────────────────────────────────────────────────┘

Should see: "Registered tunnel connection"
If working, press Ctrl+C to stop


Step 8: Install as Service (Auto-start on Boot)
┌─────────────────────────────────────────────────────────────┐
│ sudo nano /etc/systemd/system/cloudflared.service          │
└─────────────────────────────────────────────────────────────┘

Paste this:
┌─────────────────────────────────────────────────────────────┐
│ [Unit]                                                      │
│ Description=Cloudflare Tunnel                              │
│ After=network.target                                       │
│                                                             │
│ [Service]                                                   │
│ Type=simple                                                 │
│ User=pi                                                     │
│ ExecStart=/usr/local/bin/cloudflared tunnel run pra-tunnel │
│ Restart=always                                              │
│ RestartSec=5s                                               │
│                                                             │
│ [Install]                                                   │
│ WantedBy=multi-user.target                                 │
└─────────────────────────────────────────────────────────────┘

Save: Ctrl+X, then Y, then Enter

Enable and start:
┌─────────────────────────────────────────────────────────────┐
│ sudo systemctl daemon-reload                                │
│ sudo systemctl enable cloudflared                           │
│ sudo systemctl start cloudflared                            │
│ sudo systemctl status cloudflared                           │
└─────────────────────────────────────────────────────────────┘


Step 9: Test Your Site
┌─────────────────────────────────────────────────────────────┐
│ Open browser: https://skincares.work                        │
└─────────────────────────────────────────────────────────────┘

Should work now! ✅


🔍 TROUBLESHOOTING
───────────────────────────────────────────────────────────────

Site still can't be reached?
  → Wait 5-10 minutes for DNS propagation
  → Check: sudo systemctl status cloudflared
  → Check: docker ps (ensure pra-app is running)

502 Bad Gateway?
  → Check Flask app: docker logs pra-app
  → Restart app: docker restart pra-app

Tunnel won't start?
  → Check logs: sudo journalctl -u cloudflared -f
  → Verify tunnel ID matches in config.yml

Certificate error?
  → Re-authenticate: cloudflared tunnel login


📋 USEFUL COMMANDS
───────────────────────────────────────────────────────────────

Check tunnel status:
  sudo systemctl status cloudflared

View tunnel logs:
  sudo journalctl -u cloudflared -f

Restart tunnel:
  sudo systemctl restart cloudflared

Check Flask app:
  docker logs pra-app
  curl http://localhost:5001/health

List your tunnels:
  cloudflared tunnel list


✅ EXPECTED RESULT
───────────────────────────────────────────────────────────────

After setup:
  ✓ https://skincares.work works
  ✓ https://www.skincares.work works
  ✓ Free SSL/TLS (automatic)
  ✓ CDN enabled (automatic)
  ✓ DDoS protection (automatic)
  ✓ No monthly fees
  ✓ Persistent URL (never changes)


═══════════════════════════════════════════════════════════════
For detailed instructions: CLOUDFLARE_TUNNEL_SETUP.md
═══════════════════════════════════════════════════════════════
